export const API_URL = `${process.env.API_URL}`;
export const CDN_URL = 'https://cdn.yag.im';

export const APP_ENV = `${process.env.APP_ENV}`;

export const APP_TITLE = 'yag.im';
export const APP_DESCRIPTION = `${APP_TITLE}: largest digital edutainment and adventure games museum!`;

export const GA_ID = `${process.env.GA_ID}`; // google analytics id
export const ADSENSE_CLIENT_ID = `${process.env.ADSENSE_CLIENT_ID}`; // google adsense publisher id (ca-pub-...)
export const ADSENSE_HEADER_SLOT = `${process.env.ADSENSE_HEADER_SLOT}`; // header banner ad unit slot id (digits only)
